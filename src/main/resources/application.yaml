spring:
  application:
    name: talkmate-ai

  datasource:
    url: jdbc:postgresql://localhost:5432/TalkMateAI
    username: postgres
    password: 3092

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
